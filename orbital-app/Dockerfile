# REMOVED: Desktop Dockerfile intentionally removed.
# Desktop Compose apps target native installers and should not be shipped as containers.
# If you need a CI artifact to run headless smoke tests, add a CI-specific Dockerfile under ci/docker.
