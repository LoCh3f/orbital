{
  "name": "orbital",
  "private": true,
  "description": "Commit-message linting tooling for the Orbital monorepo",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
